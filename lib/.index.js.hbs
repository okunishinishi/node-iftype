/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

"use strict";

var IfType = require('./iftype'),
    create = require('./create');

var lib = create.bind();
lib.create = create;
lib.IfType = IfType;
module.exports = lib;
